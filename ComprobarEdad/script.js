//Ejecutar el siguiente programa para comprobar edad de los votantes.

//creamos variables
let edad
let final

//le pedimos que el usuario introdusca la edad
/*do{
    edad = prompt("introduce tu edad para votar")
     if(isNaN(Number(edad))){
       final = alert("error")
    } else if(edad < 18){
        final = (edad < 18)
        final= alert("menor que 18, no puedes votar")
    /*}else(edad > 18){
        final =(edad < 18)
        final = alert("puedes votar")
    }
}while(isNaN(Number(edad)))
edad = parseInt(Number(edad))*/


//agregamos una condicional
//mirar esto